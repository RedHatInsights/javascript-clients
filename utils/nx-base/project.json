{
  "name": "@redhat-cloud-services/CLIENTNAME-client",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/CLIENTNAME",
  "projectType": "library",
  "tags": [],
  "targets": {
    "clean-generate": {
      "command": "rm -rf packages/CLIENTNAME/src"
    },
    "generate": {
      "dependsOn": ["clean-generate"],
      "executor": "@redhat-cloud-services/build-utils:client-generator",
      "options": {
        "outputPath": "packages/CLIENTNAME/src",
        "specs": {
          "default": "path-to-your-openAPI-spec"
        },
        "clientName": "CLIENTNAMEClient"
      }
    },
    "clean-build": {
      "command": "rm -rf packages/CLIENTNAME/dist"
    },
    "build": {
      "dependsOn": ["clean-build", "^build"],
      "executor": "@redhat-cloud-services/build-utils:builder",
      "options": {
        "inputPath": "packages/CLIENTNAME/src",
        "outputPath": "packages/CLIENTNAME/dist",
        "main": "packages/CLIENTNAME/src/index.ts",
        "esmTsConfig": "packages/CLIENTNAME/tsconfig.esm.json",
        "cjsTsConfig": "packages/CLIENTNAME/tsconfig.cjs.json"
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs @redhat-cloud-services/CLIENTNAME-client {args.ver} {args.tag}",
      "dependsOn": ["build"]
    },
    "lint": {
      "options": {
        "args": ["packages/CLIENTNAME/**/*.ts", "packages/CLIENTNAME/*.json"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/CLIENTNAME/jest.config.ts"
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "push": true,
        "preset": "conventionalcommits"
      }
    },
    "github": {
      "executor": "@jscutlery/semver:github",
      "options": {
        "tag": "{tag}",
        "notes": "{notes}",
        "commitMessageFormat": "release: bump {projectName} to {version} [skip ci]"
      }
    },
    "npm": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public"
      }
    }
  }
}
