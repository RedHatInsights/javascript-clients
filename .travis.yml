language: node_js
cache:
  directories:
  - "$HOME/.npm"
node_js:
- '10'
install:
  - npm ci
before_script:
  - npm config set '//registry.npmjs.org/:_authToken' "${NPM_TOKEN}"
after_success:
  - npm run release
